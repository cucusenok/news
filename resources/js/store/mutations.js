import * as types from './types';

export default {
/*    [types.SET_ALL_POSTS]: (state, payload) => {
        state.all_posts = payload;
    },

    increment (state) {
        state.count++
    }*/

};